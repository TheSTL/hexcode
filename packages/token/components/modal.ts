import { color } from '../global/color';

export const modal = {
    size: {
        auto: {},
        sm: {
            width: '484px',
        }
    },
    body: {
        color: color.black[550]
    }
}
